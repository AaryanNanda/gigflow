version: '3.8'

services:
  api:
    build: .
    container_name: gigflow_backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://admin:Password%40123@gigflow-cluster.rmzvqyz.mongodb.net/gigflow?retryWrites=true&w=majority&appName=GigFlow-Cluster
      - JWT_SECRET=your_super_secret_key_123456!